import ws from 'src/services/ws.service';

// export default {
//   ws.listen(message', (payload) => {
//     // je fais des truc avec ma payload en fonction du payload.id
//   })
//   ws.listen(error', (payload) => {
//     // je fais des truc avec ma payload en fonction du payload.id
//   })
//   ws.listen(infos', (payload) => {
//     // je fais des truc avec ma payload en fonction du payload.id
//   })
// }
